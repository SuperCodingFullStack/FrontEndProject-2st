import React from 'react';
import LeftMenu from './Bottom/LeftMenu';

const BottomMenu = ({ idx }) => {
  return (
    <div>
      <LeftMenu idx={idx} />
    </div>
  );
};

export default BottomMenu;
